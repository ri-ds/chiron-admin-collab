BUILD_TAG=1  # can be anything, simulates Jenkins build
CHIRON_AUTH= # replace with your auth token
DB_HOST=host.docker.internal
DB_USER=postgres
DB_PASSWORD=chiron_demo
DB_NAME=chiron_demo
CROSS_ORIGIN_URL=http://localhost:3000

# do not modify below
SQL_ALCHEMY_URI="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:5432/${DB_NAME}"
# SQL_ALCHEMY_URI="postgresql://postgres:chiron_demo@${DB_HOST}:5432/${DB_NAME}"